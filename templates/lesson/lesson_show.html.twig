{% extends 'base.html.twig' %}
{% block body %}
    <div class="page-content bg-white">
        <!-- inner page banner -->
        <div class="page-banner ovbl-dark" style="background-image:url(assets/images/banner/banner2.jpg);">
            <div class="container">
                <div class="page-banner-entry">
                    <h1 class="text-white">Lesson : {{lesson.title}}</h1>
				 </div>
            </div>
        </div>
		<!-- Breadcrumb row -->
		<div class="breadcrumb-row">
			<div class="container">
				<ul class="list-inline">
					<li><a href="{{path('app_home')}}">Home</a></li>
					<li><a href="{{path('app_lessons')}}">Online Lessons</a></li>
					<li>{{lesson.title}}</li>
				</ul>
			</div>
		</div>
		<!-- Breadcrumb row END -->
        <!-- inner page banner END -->
		<div class="content-block">
            <!-- About Us -->
			<div class="section-area section-sp1">
                <div class="container">
					 <div class="row d-flex flex-row-reverse">
						{# <div class="col-lg-3 col-md-4 col-sm-12 m-b30">
							<div class="course-detail-bx">
								<div class="course-buy-now text-center">
									<a href="#" class="btn radius-xl text-uppercase">Buy Now This Courses</a>
								</div>
							</div>
						</div> #}
					
						{# <div class="col-lg-9 col-md-8 col-sm-12"> #}
						<div class="col-12">
							<div class="courses-post">
								

								<div class="ttr-post-media media-effect">
									<a href="{{lesson.videoUrl}}" class="left-div glightbox col-12 col-md-6 col-lg-6 w-50 mb-3">
										<img height="300" alt="" src="{{asset(lesson.img ? 'uploads/'~ lesson.img : 'images/event/pic4.jpg' )}}" alt="">
										{# <img class="iconplay" alt="" src="{{asset('images/templates/iconplay.svg')}}"/> #}
										<i class="bi bi-play-circle"></i>
									</a>
								</div>

								<div class="ttr-post-info">
									<div class="ttr-post-title ">
										<h2 class="post-title">{{lesson.title}}</h2>
									</div>
								</div>
							</div>
							<div class="courese-overview" id="overview">
								<div class="row">
									<div class="col-12">
										{{lesson.content|raw}}
									</div>
								</div>
							</div>
							
						</div>
						
					</div>
				</div>
            </div>
        </div>
		<!-- contact area END -->
		
    </div>

{% endblock %}